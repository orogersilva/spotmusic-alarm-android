<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.orogersilva.spotmusicalarm.featuredashboard">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application>

        <!-- ACTIVITIES - BEGIN -->

        <activity
            android:name=".presentation.screen.clockalarmmanager.view.ClockAlarmManagerActivity"
            android:screenOrientation="portrait"
            >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name="com.spotify.sdk.android.authentication.LoginActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:screenOrientation="portrait"
            />

        <activity
            android:name=".presentation.screen.newclockalarm.view.NewClockAlarmActivity"
            android:screenOrientation="portrait"
            />

        <activity
            android:name=".presentation.screen.playlist.view.PlaylistActivity"
            android:screenOrientation="portrait"
            />

        <activity
            android:name=".presentation.screen.tracklist.view.TrackListActivity"
            android:screenOrientation="portrait"
            />

        <!-- ACTIVITIES - END -->

    </application>

</manifest>
